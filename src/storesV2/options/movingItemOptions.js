// These are referenced in the logic related to moving start/finish flag, so create variables to avoid referencing by ID
export const noMovingItem = null
export const startFlag = 1
export const finishFlag = 2
